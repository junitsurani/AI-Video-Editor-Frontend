# Private Flask address used by Next.js on the server, at build time and runtime.
API_INTERNAL_URL=http://127.0.0.1:5001

# Temporary dashboard testing without Flask auth (currently defaults to true).
# Set false and rebuild/restart the frontend to reconnect the backend.
NEXT_PUBLIC_FRAME_DEMO_MODE=true
